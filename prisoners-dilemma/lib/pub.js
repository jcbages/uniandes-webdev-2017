/*import { Matches } from '../imports/api/MatchDB.jsx';
import { Users } from '../imports/api/UsersDB.jsx';

if (Meteor.isServer) {
	Meteor.publish('Matches', () => {
		return Matches.find();
	});
	Meteor.publish('Users', () => {
		return Users.find();
	});
	Meteor.publish('internalUsers', () => {
		return Users.find();
	});
}*/